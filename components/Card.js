import React from 'react';
import { StyleSheet, Text} from 'react-native';

const Card = props => {
    return(
        <Text>CardDummy</Text>
    );
}

const styles = StyleSheet.create({

});

export default Card;