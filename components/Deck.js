import React from 'react';
import { StyleSheet, Text} from 'react-native';

const Deck = props => {
    return(
        <Text>Deck</Text>
    );
}

const styles = StyleSheet.create({

});

export default Deck;