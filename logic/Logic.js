//debug logic

// todo everything

export default class Logic{
    static getTitleById = id =>{
        if(id == 1)
            return "Algebra Deck"
        if(id == 2)
            return "Programming Deck"
    }

    static getObjectById(id){

    }

    static fetchAll(){
        
    }
}